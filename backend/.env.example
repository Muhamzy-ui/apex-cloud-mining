# Backend Environment Variables Template
# Copy this to .env and fill in actual values

# Django Settings
SECRET_KEY=your-secret-key-here-generate-with-secrets.token_urlsafe(50)
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com,www.yourdomain.com

# Database (PostgreSQL)
DB_HOST=localhost
DB_NAME=apex_db
DB_USER=postgres
DB_PASSWORD=Apex123
DB_PORT=5432

# Redis (for Celery)
REDIS_URL=redis://your-render-redis-host:6379/0

# CORS Settings (allow your frontend domain)
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,http://localhost:3000,http://localhost:5173

# Payment & Banking
APEX_USDT_WALLET=TQnXpzuRr8PFRnKqDU5b7ZfmwdJGFH4kLm
APEX_BANK_NAME=Opay
APEX_BANK_ACCOUNT_NAME=Apex Cloud Mining Ltd
APEX_BANK_ACCOUNT_NUMBER=0123456789
APEX_MIN_WITHDRAWAL_USDT=10.0
APEX_REFERRAL_COMMISSION_PCT=10.0
APEX_USD_TO_NGN_RATE=1450.0
APEX_USD_TO_GHS_RATE=15.5

# Paystack API (for account verification in Nigeria)
PAYSTACK_SECRET_KEY=sk_live_xxx

# Email (optional, for notifications)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# AWS S3 (optional, if using Cloudinary skip this)
# AWS_ACCESS_KEY_ID=xxx
# AWS_SECRET_ACCESS_KEY=xxx
# AWS_STORAGE_BUCKET_NAME=xxx
